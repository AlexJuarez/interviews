const twoSumClosest = (nums, target) => {
  const n = Math.floor(nums.length / 2);
  const m = nums.length - n;
  const D = new Array(n).fill(0).map(() => new Array(m));

  for (let i = 0; i < n; i++) {
    for (let j = 0; j < m; j++) {

    }
  }
};